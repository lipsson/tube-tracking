export type HeaderType = {
    helmetText: string;
    title: string;
    subtitle?: string;
    buttonText?: string;
    buttonHref?: string;
    disabled?: boolean;
};

export type ContentType = {
    heading: string;
    subHeading?: string;
    docs?: string;
}